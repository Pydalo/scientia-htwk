const config = {
    port: 3000,
    host: "localhost",
    windows: true,
    /*
    Debug Level:
    -1 - debug disabled
    0 - debug all
    1 - network-debug disabled
    */
    debuglevel: 1 
};

export default config;